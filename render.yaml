services:
  - type: web
    name: arcs-backend
    env: docker
    plan: free
    region: oregon
    dockerFilePath: Dockerfile
    envVars:
      - key: PORT
        value: 7860
      - key: GROQ_API_KEY
        sync: true
      - key: TAVILY_API_KEY
        sync: true
